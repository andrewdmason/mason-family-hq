// Streams the app shell immediately and holds the content area while this
// section's data loads — see src/components/layout/page-loading.tsx.
export { default } from "@/components/layout/page-loading";
