/** Storage bucket holding uploaded book files and their converted HTML. */
export const READING_BOOKS_BUCKET = "reading-books";

/** Storage bucket holding parent-uploaded milestone reward images. */
export const READING_MILESTONES_BUCKET = "reading-milestones";

/** Words a minute the reader's "time left" estimates assume — the usual figure
 * for an adult reading prose. Deliberately not personalised: a steady number
 * that's wrong the same way every time beats one that jumps as you read. */
export const READING_WORDS_PER_MINUTE = 250;
