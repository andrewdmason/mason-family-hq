/** Storage bucket for derived swing artifacts — importable from both sides
 * (queries.ts is server-only; the upload client needs the same constant). */
export const SWING_BUCKET = "swing-artifacts";
