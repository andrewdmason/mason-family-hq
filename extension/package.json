{
  "name": "mason-reader-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chrome extension that saves the current article to the Mason reader.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "dompurify": "^3.2.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2"
  }
}
