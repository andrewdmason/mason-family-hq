"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"
file_include_globs = ""

[scripts]
setup = "ln -sf \"$CONDUCTOR_ROOT_PATH/.env.local\" .env.local && npm install"
run = "npm run dev"
run_mode = "nonconcurrent"
